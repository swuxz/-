#include <stdio.h>
#include <math.h>

int main() {
    int x1, y1, x2, y2;
    double dx, dy, length;

    scanf("%d %d %d %d", &x1, &y1, &x2, &y2);

    dx = (double)x2 - x1;
    dy = (double)y2 - y1;

    length = sqrt(dx * dx + dy * dy);

    printf("%.6f\n", length);

    return 0;
}
